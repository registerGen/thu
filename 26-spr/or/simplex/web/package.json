{
  "name": "simplex-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build .. --target web --no-default-features --features web && rm -rf pkg && mv ../pkg pkg",
    "dev": "pnpm wasm && vite",
    "build": "pnpm wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-wasm": "^3.3.0",
    "vite-plugin-top-level-await": "^1.4.0"
  }
}
